DROP TABLE IF EXISTS Platform;

CREATE TABLE IF NOT EXISTS Platform(
        PlatformId      INTEGER PRIMARY KEY,
        SRId            TEXT,
        PlatformName    TEXT,
        ShortName       TEXT
        );
